name: Plugin CI

on:
  push:
    branches: [main]
    paths:
      - 'plugin/**'
      - '.github/workflows/plugin-ci.yml'
      - '.github/actions/plugin/**.yml'
  pull_request:
    paths:
      - 'plugin/**'
      - '.github/workflows/plugin-ci.yml'
      - '.github/actions/plugin/**.yml'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check formatting
        uses: ./.github/actions/plugin/lint
        with:
          source-dir: plugin
